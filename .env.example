# Termote Environment
# Copy to .env and customize

# Workspace directory to mount (only this folder is accessible)
WORKSPACE=/home/your-username/projects

# User/Group ID for file permissions (run: id -u && id -g)
UID=1000
GID=1000

# Basic auth credentials (generate with: echo "admin:$(openssl passwd -apr1)" > .htpasswd)
